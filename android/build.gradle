// Top-level build.gradle — plugin versions only; repositories live in settings.gradle.
// AGP 9 compiles Kotlin itself, so there is no separate Kotlin plugin.
plugins {
    id 'com.android.application' version '9.4.0' apply false
}
