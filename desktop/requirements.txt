# Runtime dependencies for running from a source checkout (python run.py).
# Kept in sync with pyproject.toml. Optional RVM segmentation: pip install -e ".[rvm]"
numpy>=1.24
opencv-python>=4.8
pyvirtualcam>=0.11
mediapipe>=0.10.14
pillow>=10.0
rich>=13.0
